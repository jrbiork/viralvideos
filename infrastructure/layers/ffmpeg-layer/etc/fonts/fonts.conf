<?xml version="1.0"?>
<!DOCTYPE fontconfig SYSTEM "fonts.dtd">
<fontconfig>
  <!-- Font directory -->
  <dir>/opt/fonts</dir>

  <!-- Cache directory -->
  <cachedir>/tmp/fontconfig</cachedir>

  <!-- Periodic rescans -->
  <config>
    <rescan>5</rescan>
  </config>

  <!-- Generic fallbacks -->
  <alias>
    <family>sans-serif</family>
    <prefer>
      <family>DMSerifText</family>
    </prefer>
  </alias>

  <alias>
    <family>serif</family>
    <prefer>
      <family>DMSerifText</family>
    </prefer>
  </alias>

  <alias>
    <family>monospace</family>
    <prefer>
      <family>DMSerifText</family>
    </prefer>
  </alias>

  <!-- DMSerifText variants -->
  <alias>
    <family>DMSerifText</family>
    <prefer>
      <family>DMSerifText</family>
    </prefer>
  </alias>

  <alias>
    <family>DMSerifText-Regular</family>
    <prefer>
      <family>DMSerifText-Regular</family>
      <family>DMSerifText</family>
    </prefer>
  </alias>

  <!-- Rendering tweaks -->
  <match target="font">
    <edit name="rgba" mode="assign">
      <const>rgb</const>
    </edit>
  </match>

  <match target="font">
    <edit name="hinting" mode="assign">
      <bool>true</bool>
    </edit>
  </match>

  <match target="font">
    <edit name="antialias" mode="assign">
      <bool>true</bool>
    </edit>
  </match>
</fontconfig>