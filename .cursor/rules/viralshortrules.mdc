---
description: Project-wide rules (Next.js + AWS)
globs: '*'
alwaysApply: true
---

- Don’t re-verify the JWT signature in the lambda handler
- Do NOT create new tsconfig.json and package.json for this new lambda. use the ones from lambda folder
- in the auth token there are only sub and username fields available
- always use ./deploy.sh to deploy the application
